package version

// Version is the current version of the caldav-cli tool.
var Version = "0.1.0"

// Commit is the git commit hash at build time.
var Commit = "unknown"

// BuildTime is the timestamp when the binary was built.
var BuildTime = "unknown"
